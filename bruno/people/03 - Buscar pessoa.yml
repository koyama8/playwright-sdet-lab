info:
  name: Buscar pessoa criada
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/people/{{personId}}"
  auth:
    type: bearer
    token: "{{accessToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve buscar a pessoa pelo id", () => {
          expect(res.getStatus()).to.equal(200);
          expect(res.getBody().data.id).to.equal(bru.getVar("personId"));
        });

settings:
  encodeUrl: true
  timeout: 10000
  followRedirects: true
  maxRedirects: 5
