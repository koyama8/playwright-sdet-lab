info:
  name: Listar pessoas
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/people?page=1&pageSize=20"
  auth:
    type: bearer
    token: "{{accessToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve listar pessoas com paginação", () => {
          expect(res.getStatus()).to.equal(200);
          expect(res.getBody().data.items).to.be.an("array");
          expect(res.getBody().data.pagination.page).to.equal(1);
        });

settings:
  encodeUrl: true
  timeout: 10000
  followRedirects: true
  maxRedirects: 5
