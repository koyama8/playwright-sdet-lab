info:
  name: Excluir pessoa criada
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/api/people/{{personId}}"
  auth:
    type: bearer
    token: "{{accessToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve excluir a pessoa", () => {
          expect(res.getStatus()).to.equal(204);
        });

settings:
  encodeUrl: true
  timeout: 10000
  followRedirects: true
  maxRedirects: 5
