info:
  name: Login inválido
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "qa@adminlab.com",
        "password": "senha-incorreta"
      }
  auth: none

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve rejeitar credenciais inválidas", () => {
          expect(res.getStatus()).to.equal(401);
          expect(res.getBody().error).to.equal("Invalid email or password");
        });

settings:
  encodeUrl: true
  timeout: 10000
  followRedirects: true
  maxRedirects: 5
