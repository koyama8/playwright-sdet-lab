info:
  name: Encerrar sessão
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/api/auth/logout"
  body:
    type: json
    data: |-
      {
        "refreshToken": "{{refreshToken}}"
      }
  auth: none

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve revogar o refresh token", () => {
          expect(res.getStatus()).to.equal(204);
        });

settings:
  encodeUrl: true
  timeout: 10000
  followRedirects: true
  maxRedirects: 5
