info:
  name: Excluir filme criado
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseUrl}}/api/movies/{{movieId}}"
  auth:
    type: bearer
    token: "{{accessToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve excluir o filme", () => {
          expect(res.getStatus()).to.equal(204);
        });

settings:
  encodeUrl: true
  timeout: 10000
  followRedirects: true
  maxRedirects: 5
