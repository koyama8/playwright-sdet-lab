info:
  name: Rejeitar paginação inválida
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/api/movies?page=0&pageSize=500"
  auth:
    type: bearer
    token: "{{accessToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve limitar parâmetros de paginação", () => {
          expect(res.getStatus()).to.equal(400);
          expect(res.getBody().error.code).to.equal("VALIDATION_ERROR");
        });

settings:
  encodeUrl: true
  timeout: 10000
  followRedirects: true
  maxRedirects: 5
