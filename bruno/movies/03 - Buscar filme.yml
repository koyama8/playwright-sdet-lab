info:
  name: Buscar filme criado
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/movies/{{movieId}}"
  auth:
    type: bearer
    token: "{{accessToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve buscar o filme pelo id", () => {
          expect(res.getStatus()).to.equal(200);
          expect(res.getBody().data.id).to.equal(bru.getVar("movieId"));
        });

settings:
  encodeUrl: true
  timeout: 10000
  followRedirects: true
  maxRedirects: 5
